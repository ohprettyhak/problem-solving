#include <bits/stdc++.h>

#define FASTIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

using namespace std;

int main() {
    FASTIO;

    long long n, m;
    cin >> n >> m;

    cout << n * m / 2 << '\n';
    
    return 0;
}